- مدیر می‌تواند کل اطلاعات را در فایل JSON ذخیره کند.
- مدیر می‌تواند فایل JSON را انتخاب و روی پایگاه داده سرور بازیابی کند.
- ساختار فایل و تعداد رکوردها پیش از بازیابی کنترل و نمایش داده می‌شود.
- پیش از بازیابی، فایل پشتیبان خودکار از وضعیت فعلی دانلود می‌شود.
- نسخه قبلی در SQLite سرور نیز نگهداری می‌شود.
